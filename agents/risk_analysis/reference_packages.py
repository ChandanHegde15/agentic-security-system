"""Small experiment reference catalogues; coverage limits similarity detection."""

DEFAULT_REFERENCE_PACKAGES: dict[str, tuple[str, ...]] = {
    "pypi": ("requests", "numpy", "pandas", "flask"),
    "npm": ("express", "axios", "react"),
}
